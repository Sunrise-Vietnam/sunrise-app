import React from 'react';
import StudyProgress from './StudyProgress';
import Routes from '../';

const StudyProgressContainer = (props) => {
    return (
        <StudyProgress/>
    );
};

export default StudyProgressContainer;
