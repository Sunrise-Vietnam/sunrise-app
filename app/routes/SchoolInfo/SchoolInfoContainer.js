import React from 'react';
import SchoolInfo from './SchoolInfo.js';
import Routes from '../';

const SchoolInfoContainer = (props) => {
    return (
        <SchoolInfo
            {...props}
        />
    );
};

export default SchoolInfoContainer;